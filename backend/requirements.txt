# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database (SQLite for Cloud Run simplicity)
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[argon2]>=1.7.4
argon2-cffi>=23.1.0

# Validation
pydantic>=2.0.0
email-validator>=2.0.0

# Image processing
pillow>=10.0.0

# Utilities
python-dotenv>=1.0.0

# Note: torch/torchvision installed in Dockerfile for CPU-only
